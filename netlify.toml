# Netlify Configuration for COPARMEX Website
[build]
  command = "echo 'Static site - no build required'"
  publish = "public"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  command = "python -m http.server 8000"
  port = 8000
